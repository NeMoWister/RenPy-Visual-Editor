
USER_GUIDE_MARKDOWN = r"""
# Руководство пользователя - RenPy Visual Script Editor

Этот редактор позволяет собирать сценарии для Ren'Py визуально, узел за узлом,
не печатая код вручную, и сразу видеть, как сцена будет выглядеть в игре.

По вопросам или с предложением можно обратиться сюда: https://discord.com/invite/XsCq2ndRGX

Скачать необходимые ресурсы для создания модификаций по бесконечному лету можно тут: https://disk.yandex.ru/d/SqnaZquLyvZ6-Q

Их необходимо распаковать в папку с приложением. После этого импортируйте characters.json и resources_config.json в соответствующих окнах.

Важно: после импорта переменных сохраните проект, чтобы они были сохранены.

---

## 1. Общая структура

- **Проект** состоит из **сцен**. Каждая сцена - это список **узлов** (нод).
- Узел - это одна строка действия: показать фон, сказать реплику, проиграть
  музыку и так далее.
- Слева - список сцен и список узлов текущей сцены.
- В центре - панель редактирования выбранного узла.
- Справа - **живой предпросмотр сцены**: как всё будет выглядеть на экране
  на момент выбранного узла.

---

## 2. Структура папки ресурсов

Ресурсы лежат в папке `resources/`, рядом с программой, и делятся на
**два источника**:

```
resources/default/bg|cg|sprites|music|sounds/...
resources/custom/bg|cg|sprites|music|sounds/...
```

Оба источника работают в редакторе одинаково - видны в каруселях выбора,
используются в превью и в самом сценарии. **Разница только в одном месте:**
в блок объявлений (`image .../define ...`, кнопка «Экспорт defines
ресурсов») попадают **только** ресурсы из `custom/`. Ресурсы из `default/`
считаются уже объявленными где-то ещё (например, в базовом шаблоне проекта)
и не дублируются в коде. Это нужно для бесшовной работы с модами.

Если ресурс не нужен в коде объявлений - кладите его в `default/`.
Если нужен - в `custom/`.

### Автоматические имена ресурсов

Редактор автоматически формирует имя переменной по имени файла. Правила разные для
разных категорий:

| Категория | Файл | Автоматическое имя |
|---|---|---|
| Фон (bg) | `bg/bus_stop.jpg` | `bg bus_stop` (с пробелом) |
| CG | `cg/d1_food_normal.jpg` | `cg d1_food_normal` (с пробелом) |
| Спрайты | `sprites/us/normal/smile.png` | `us_normal_smile` (через подчёркивание) |
| Музыка | `music/name.ogg` | `music_list["name"]` - ссылка на словарь треков, который должен существовать в вашем проекте Ren'Py; редактор его не создаёт |
| Звуки | `sounds/achievement.ogg` | `sfx_achievement` |

Если автоматическое имя не подходит - задайте своё в диалоге **Проект →
Настройки ресурсов → Переопределения имён**. Там же можно:
- **Сохранить переопределения** - применить то, что введено в таблице.
- **Экспорт в файл / Импорт из файла** - перенести весь набор своих имён
  в другой проект или сделать резервную копию.
- **Сбросить переопределения** - удалить все свои имена, вернуться к
  автоматическим (действие нельзя отменить).

---

## 3. Составные спрайты (sprites.rpy)

Если в `resources/default/sprites/` или `resources/custom/sprites/` лежит
файл **sprites.rpy** с описанием составных спрайтов (персонаж собран из
нескольких слоёв - тело + предмет + эмоция, как делает `im.Composite` в
Ren'Py), редактор сам разбирает этот файл и показывает такие спрайты
отдельным блоком в узле «Показать спрайт»: персонаж → дальность плана
(far/close/normal) → эмоция, с миниатюрой, где все слои уже наложены друг
на друга - ровно так, как это будет выглядеть в игре.

Условную раскраску по времени суток (`persistent.sprite_time`,
`im.matrix.tint`) редактор не показывает в превью - это игровая логика,
а не редакторская; в превью всегда показывается базовый, нетонированный
вариант.

Эти спрайты уже объявлены в самом `sprites.rpy`, поэтому редактор **не**
дублирует их в блоке `define`/`image`.

**Скрытие** составного спрайта - по персонажу: `hide cs`, а не по полному
имени эмоции. В узле «Скрыть спрайт» для этого есть отдельный блок
«Скрыть персонажа целиком» - один клик по папке персонажа, без захода внутрь.

Обычные (не составные) спрайты в той же папке `sprites/` продолжают
работать как раньше - оба способа можно использовать одновременно.

---

## 4. Типы узлов

| Узел | Что делает |
|---|---|
| 💬 Диалог | Реплика персонажа. Выберите персонажа и впишите текст. |
| 📖 Нарратор | Реплика без персонажа (текст от лица рассказчика). |
| 🖼 Фон (scene) | Показывает фон. Можно выбрать переход (dissolve, fade, и т.д.). |
| 🎨 CG (show) | Показывает полноэкранную CG-картинку поверх фона. |
| 👤 Показать спрайт | Показывает спрайт персонажа (обычный или составной), с позицией по горизонтали и переходом. |
| ❌ Скрыть спрайт | Скрывает спрайт - конкретный или персонажа целиком. |
| 🎵 Музыка | `play music ...` |
| 🔊 Звук | `play sound ...` |
| 🏷 Метка (label) | Вставляет `label имя:` - точка, на которую можно сделать `jump`/`call`. |
| ↪ Переход (jump) | `jump имя_метки` |
| 📋 Меню выбора | Меню с вариантами ответа, у каждого варианта свой переход (см. ниже). |
| ⏸ Пауза | `pause` - ждёт клика игрока (0 секунд) либо заданное время. |
| ⏹ Return | Просто `return`, без параметров. |
| 🐍 Python код | Произвольная строка/блок Python-кода (`$ ...` или `python: ...`). |

### Меню выбора: jump или call?

У каждого варианта меню есть галочка **«call»**. По умолчанию она выключена,
переход на метку делается через `jump`. Разница важна, если внутри метки
есть `return`:

- **jump** - просто переходит на метку и забывает, откуда пришёл. Если в
  той метке встретится `return`, Ren'Py решит, что сценарий закончился, и
  игра выйдет в **главное меню**.
- **call** - переходит на метку, но запоминает место вызова. После `return`
  игра вернётся обратно, на следующую строку после этого варианта меню.

Включайте «call», если метка должна вернуть игрока в это же меню после
`return`, а не выкинуть в главное меню.

### Переходы (transitions)

Доступны: `dissolve`, `fade`, `pixellate`, `blinds`, `squares`, `wipeleft`,
`wiperight`, `wipeup`, `wipedown`, `dspr`,
и пустой вариант - «без перехода».

Если несколько узлов «Показать спрайт» идут **подряд** и у них **одинаковый**
переход - при экспорте они объединяются в один блок:

```
show персонаж1
show персонаж2
with dissolve
```

вместо двух отдельных `with dissolve` - так выходят на сцену сразу несколько
персонажей одним плавным переходом, как это и предусмотрено в Ren'Py.

---

## 5. Предпросмотр сцены

Справа - окно, показывающее, как выглядит сцена на момент выбранного узла:
фон, CG, все спрайты, которые сейчас должны быть на экране, и текущая
реплика.

- **Перетащить спрайт мышью** - меняет его позицию по горизонтали (xalign),
  значение сразу обновляется в панели редактирования узла.
- **Кликнуть по спрайту без перетаскивания** - удаляет из сцены тот узел,
  который вывел этот спрайт на экран. Если спрайт был показан несколько
  шагов назад - найдётся и удалится именно тот узел показа, список слева
  обновится автоматически.
- При наведении курсора на спрайт он подсвечивается рамкой с подсказкой «✕ удалить».

---

## 6. Персонажи

**Проект → Персонажи...** - список персонажей сценария (имя, переменная,
цвет реплики). Кнопки:

- **+ Добавить / ✕ Удалить** - обычное управление списком.
- **Экспорт... / Импорт...** - сохранить весь список персонажей в отдельный
  JSON-файл (для переноса в другой проект или резервной копии) и загрузить
  его обратно. При импорте можно либо добавить к текущим (совпадающие по
  переменной - перезапишутся), либо полностью заменить список.
- **Сбросить список** - удаляет всех персонажей из текущего проекта
  (с подтверждением).

---

## 7. Генерация и экспорт кода

- **Генерация → Просмотр кода** - показывает итоговый `.rpy`-код
  без сохранения на диск, для проверки перед экспортом.
- **Генерация → Экспорт .rpy** - сохраняет сценарий в файл.
- **Генерация → Экспорт блока defines** - отдельно персонажи (`define ... =
  Character(...)`).
- **Генерация → Экспорт defines ресурсов** - отдельно объявления `image`/
  `define` для ресурсов из `custom/` (см. раздел 2).

---

## 8. Частые вопросы

**Я добавил файлы в `resources/`, но их не видно в редакторе.**
Нажмите «Переиндексировать ресурсы» или перезапустите редактор, чтобы обновить индекс ресурсов.

**Почему мой спрайт не скрывается командой `hide`, которую сгенерировал
редактор?**
Для составных спрайтов (sprites.rpy) тегом всегда является имя персонажа
(первое слово), а не полное имя эмоции - `hide cs`, а не
`hide cs normal stethoscope far`. Это сделано специально, см. раздел 3.

**Что будет, если в проекте лежит старый файл с именами ресурсов в старом
формате?**
Если ресурсы были переименованы предыдущей версией редактора, имена в уже
сохранённых сценах могут не совпасть с новыми автоматическими именами.
В этом случае откройте узел, который ссылается на такой ресурс, и выберите
его заново через карусель - редактор подставит актуальное имя.
"""
